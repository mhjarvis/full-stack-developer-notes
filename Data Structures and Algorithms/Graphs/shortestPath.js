/*



*/

const edges = [
    ['w', 'x'],
    ['x', 'y'], 
    ['z', 'y'],
    ['z', 'v'],
    ['w', 'v']
]

const toGraph = (edges) => {
    const graph = {};           // create graph object to hold adjacency list




    return graph;
}

console.log(toGraph(edges));